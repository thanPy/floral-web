[build]
  base = "artifacts/bunga-cinta"
  command = "pnpm install --frozen-lockfile && pnpm run build"
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
